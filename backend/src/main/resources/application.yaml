spring:
  application:
    name: QuickTrade
  datasource:
    url: jdbc:postgresql://localhost:5432/quicktrade
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080